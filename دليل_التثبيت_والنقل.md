# 📖 دليل تثبيت ونقل Add-in للمراجعة اللغوية

## 🎯 الجزء الأول: جعل التطبيق يعمل تلقائياً مع كل ملف Word

### الطريقة 1️⃣: تثبيت Add-in بشكل دائم (موصى بها)

#### الخطوات:
1. **افتح Word**
2. اذهب إلى: **ملف (File)** → **خيارات (Options)** → **الوظائف الإضافية (Add-ins)**
3. في الأسفل، اختر: **الوظائف الإضافية لـ Office (Office Add-ins)** من القائمة المنسدلة
4. اضغط **انتقال (Go)**
5. اضغط على **الوظائف الإضافية المشتركة (Shared Folder)**
6. اضغط **إضافة (Add)**
7. اختر ملف `manifest.xml` من مجلد التطبيق

**الآن التطبيق سيظهر تلقائياً في كل ملف Word تفتحه!** ✅

---

### الطريقة 2️⃣: إضافة المجلد المشترك (Shared Folder)

هذه الطريقة تجعل التطبيق متاحاً لجميع ملفات Word بشكل دائم:

#### الخطوات:

1. **أنشئ مجلد مشترك:**
   - أنشئ مجلد جديد مثلاً: `C:\WordAddins`
   - انسخ ملف `manifest.xml` إلى هذا المجلد

2. **أضف المجلد إلى Word:**
   - افتح Word
   - اذهب إلى: **ملف** → **خيارات** → **مركز التوثيق (Trust Center)**
   - اضغط **إعدادات مركز التوثيق (Trust Center Settings)**
   - اختر **كتالوجات الوظائف الإضافية الموثوقة (Trusted Add-in Catalogs)**
   - في خانة **عنوان الكتالوج (Catalog Url)**، أدخل مسار المجلد:
     ```
     C:\WordAddins
     ```
   - ضع علامة ✓ على **إظهار في القائمة (Show in Menu)**
   - اضغط **إضافة كتالوج (Add catalog)**
   - اضغط **موافق (OK)**

3. **أعد تشغيل Word**

**الآن التطبيق سيكون متاحاً دائماً في جميع ملفات Word!** 🎉

---

## 📦 الجزء الثاني: نقل التطبيق لصديقك

### ما يحتاجه صديقك:

#### 1. **الملفات الأساسية:**
قم بنسخ المجلد الكامل الذي يحتوي على:
```
📁 برنامجالأفس - Copy/
├── 📄 manifest.xml          (ملف التعريف)
├── 📄 package.json          (إعدادات المشروع)
├── 📁 src/                  (ملفات التطبيق)
│   ├── 📄 taskpane.html
│   ├── 📄 taskpane.css
│   └── 📄 taskpane.js
└── 📄 دليل_التثبيت_والنقل.md (هذا الملف)
```

#### 2. **البرامج المطلوبة:**
صديقك يحتاج تثبيت:
- ✅ **Node.js** (من https://nodejs.org)
- ✅ **Microsoft Word** (2016 أو أحدث)

---

### 📋 خطوات التثبيت لصديقك:

#### **الخطوة 1: تثبيت Node.js**
1. حمّل Node.js من: https://nodejs.org
2. ثبّت البرنامج (اضغط Next → Next → Install)
3. أعد تشغيل الكمبيوتر

#### **الخطوة 2: تثبيت المكتبات المطلوبة**
1. افتح **PowerShell** أو **Command Prompt**
2. انتقل لمجلد التطبيق:
   ```powershell
   cd "C:\المسار\إلى\المجلد"
   ```
3. ثبّت المكتبات:
   ```powershell
   npm install
   ```
4. ثبّت أداة الشهادات:
   ```powershell
   npm install -g office-addin-dev-certs
   ```
5. أنشئ الشهادة الموثوقة:
   ```powershell
   office-addin-dev-certs install
   ```

#### **الخطوة 3: تشغيل الخادم**
```powershell
npm start
```
**ملاحظة:** يجب أن يبقى هذا النافذة مفتوحة طوال استخدام التطبيق!

#### **الخطوة 4: تحميل Add-in في Word**
1. افتح Word
2. اذهب إلى: **إدراج (Insert)** → **الوظائف الإضافية (Add-ins)** → **وظائفي الإضافية (My Add-ins)**
3. اختر **الوظائف الإضافية المشتركة (Shared Folder)**
4. اختر التطبيق من القائمة
5. اضغط **إضافة (Add)**

**الآن التطبيق يعمل!** 🎊

---

## 🚀 الطريقة الأسهل: إنشاء ملف تشغيل تلقائي

سأنشئ لك ملف `.bat` يشغل الخادم تلقائياً:

### ملف `تشغيل_التطبيق.bat`:
```batch
@echo off
echo 🚀 جاري تشغيل خادم Add-in...
echo.
echo ⚠️ لا تغلق هذه النافذة!
echo.
cd /d "%~dp0"
npm start
pause
```

**كيف تستخدمه:**
1. احفظ الملف أعلاه باسم `تشغيل_التطبيق.bat` في مجلد التطبيق
2. اضغط عليه مرتين لتشغيل الخادم
3. افتح Word واستخدم التطبيق

---

## 🌐 الطريقة المتقدمة: نشر التطبيق على الإنترنت (اختياري)

إذا أردت أن يعمل التطبيق بدون تشغيل خادم محلي:

### الخيارات:
1. **GitHub Pages** (مجاني)
2. **Netlify** (مجاني)
3. **Vercel** (مجاني)

### مثال باستخدام GitHub Pages:

1. **أنشئ حساب على GitHub** (github.com)
2. **أنشئ repository جديد**
3. **ارفع الملفات:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/username/repo.git
   git push -u origin main
   ```
4. **فعّل GitHub Pages:**
   - اذهب إلى Settings → Pages
   - اختر branch: main
   - اختر folder: /src
   - احفظ

5. **عدّل manifest.xml:**
   غيّر `https://localhost:3000` إلى:
   ```
   https://username.github.io/repo
   ```

**الآن التطبيق يعمل من الإنترنت ولا يحتاج خادم محلي!** 🌍

---

## ❓ الأسئلة الشائعة

### س: هل يحتاج صديقي مفتاح API؟
**ج:** نعم، كل مستخدم يحتاج مفتاح Gemini API خاص به. يمكن الحصول عليه من:
https://makersuite.google.com/app/apikey

### س: هل يمكن استخدام التطبيق بدون إنترنت؟
**ج:** لا، التطبيق يحتاج إنترنت للاتصال بـ Gemini API.

### س: كيف أحدّث التطبيق؟
**ج:** 
1. أوقف الخادم (Ctrl+C)
2. استبدل الملفات القديمة بالجديدة
3. شغّل الخادم من جديد (`npm start`)
4. أعد تحميل Add-in في Word

### س: التطبيق لا يظهر في Word؟
**ج:** تأكد من:
- ✅ الخادم يعمل (`npm start`)
- ✅ الشهادة مثبتة (`office-addin-dev-certs install`)
- ✅ ملف `manifest.xml` صحيح
- ✅ Word تم إعادة تشغيله بعد التثبيت

---

## 📞 الدعم الفني

إذا واجهت أي مشكلة:
1. تأكد من تشغيل الخادم (`npm start`)
2. تأكد من الشهادة الموثوقة
3. راجع رسائل الخطأ في Console (F12 في Word)
4. أعد تشغيل Word

---

## ✅ خلاصة سريعة

### للاستخدام اليومي:
1. شغّل `تشغيل_التطبيق.bat`
2. افتح Word
3. استخدم التطبيق

### لنقل التطبيق:
1. انسخ المجلد كاملاً
2. ثبّت Node.js
3. نفّذ `npm install`
4. نفّذ `office-addin-dev-certs install`
5. شغّل `npm start`
6. حمّل Add-in في Word

**بالتوفيق! 🎉**
