# 🔧 إصلاح سريع للمشاكل

## المشكلة 1: الأزرار لا تعمل ❌

### السبب:
- ملف CSS تضرر أثناء التعديلات
- بعض الأزرار تحتاج إلى classes صحيحة

### الحل السريع:

#### الخطوة 1: تحديث أسماء الأزرار في HTML
افتح `taskpane.html` وتأكد من أن الأزرار لديها الـ IDs الصحيحة:

```html
<button id="btn-common-mistakes" class="tool-btn">
    <span class="icon">📝</span> الأخطاء الشائعة
</button>
<button id="btn-duplicate-check" class="tool-btn">
    <span class="icon">🔄</span> كشف التكرار
</button>
<button id="btn-text-stats" class="tool-btn">
    <span class="icon">📊</span> الإحصائيات
</button>
<button id="btn-spacing-review" class="tool-btn">
    <span class="icon">⎵</span> مراجعة المسافات
</button>
<button id="btn-quran-search" class="tool-btn quran-btn">
    <span class="icon">📖</span> البحث في القرآن
</button>
```

#### الخطوة 2: تحقق من Event Listeners في taskpane.js
تأكد من وجود هذه الأسطر في `Office.onReady`:

```javascript
document.getElementById("btn-common-mistakes").onclick = handleCommonMistakes;
document.getElementById("btn-duplicate-check").onclick = handleDuplicateCheck;
document.getElementById("btn-text-stats").onclick = handleTextStats;
document.getElementById("btn-spacing-review").onclick = handleSpacingReview;
document.getElementById("btn-quran-search").onclick = openQuranSearch;
```

---

## المشكلة 2: البحث في القرآن ليس بملء الشاشة ❌

### الحل:
أضف هذا CSS في نهاية ملف `taskpane.css`:

```css
/* Quran Modal - Fullscreen */
#quran-modal.settings-modal {
    padding-top: 0 !important;
    align-items: center !important;
}

.quran-search-modal {
    max-width: 100vw !important;
    width: 100vw !important;
    height: 100vh !important;
    max-height: 100vh !important;
    margin: 0 !important;
    border-radius: 0 !important;
    padding: 0 !important;
    display: flex !important;
    flex-direction: column !important;
}

.quran-search-modal .settings-header {
    padding: 15px 20px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    border-radius: 0;
    flex-shrink: 0;
}

.quran-search-modal .settings-header h2 {
    color: white;
    font-size: 1.3rem;
    margin: 0;
}

.quran-search-modal .close-btn {
    background: rgba(255, 255, 255, 0.2);
    color: white;
    width: 36px;
    height: 36px;
    font-size: 1.3rem;
    border-radius: 50%;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
}

.quran-search-modal .close-btn:hover {
    background: rgba(255, 255, 255, 0.3);
    transform: rotate(90deg);
}

.quran-iframe-container {
    flex: 1;
    height: 100%;
    width: 100%;
    padding: 0;
    margin: 0;
    overflow: hidden;
}

.quran-iframe-container iframe {
    width: 100% !important;
    height: 100% !important;
    border: none !important;
    border-radius: 0 !important;
    display: block;
}
```

---

## ✅ الحل الأسرع (موصى به):

### إعادة تحميل ملف CSS الأصلي:

1. احذف ملف `taskpane.css` الحالي
2. أنشئ ملف جديد باسم `taskpane.css`
3. انسخ المحتوى من النسخة الاحتياطية أو من Git

أو يمكنني إنشاء ملف CSS جديد كامل لك الآن!

---

## 🔍 للتشخيص:

افتح Developer Tools في Word (F12) وتحقق من:
1. هل هناك أخطاء في Console؟
2. هل الملفات يتم تحميلها بشكل صحيح؟
3. هل الـ IDs موجودة في HTML؟

---

## 📞 هل تريد مني:

1. ✅ إنشاء ملف CSS جديد كامل؟
2. ✅ إصلاح ملف HTML فقط؟
3. ✅ إنشاء ملف اختبار بسيط؟

**أخبرني وسأصلح المشكلة فوراً!** 🚀
